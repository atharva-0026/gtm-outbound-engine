fastapi>=0.111.0
uvicorn[standard]>=0.52.4
pandas>=2.2.0
pydantic>=2.13.5
xgboost>=2.0.0
shap>=0.45.0
scikit-learn>=1.4.0
joblib>=1.4.0
numpy>=1.26.0
chromadb>=1.5.9  # CVE-2026-45829: pre-auth RCE in chromadb's HTTP server mode, still unpatched upstream as of 1.5.9 (maintainers unresponsive per public disclosure). Not exploitable here — this repo only uses chromadb.Client() in-process, never runs the HTTP server. Revisit if a fix ships or usage ever changes to HttpClient/chroma run.
requests>=2.34.2
streamlit>=1.35.0
feedparser>=6.0.14
pytest>=8.0.0
